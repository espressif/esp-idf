dependencies:
  espressif/ethernet_init:
    version: "~1.2.0"
  espressif/esp_wifi_remote:
    version: ">=1.3.1,<2.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  espressif/esp_hosted:
    version: ">=2.11.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
