## IDF Component Manager Manifest File
dependencies:
  espressif/esp_tinyusb:
    version: "^1.3.0"
    rules:
      - if: "idf_version >=4.4"
      - if: "target in [esp32s2, esp32s3]"

  idf: "^5.0"
